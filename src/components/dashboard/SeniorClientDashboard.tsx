"use client";

import ClientDashboard from "./ClientDashboard";

// Use the same five clear priorities and large controls in simplified mode.
export default function SeniorClientDashboard() {
  return <ClientDashboard />;
}
